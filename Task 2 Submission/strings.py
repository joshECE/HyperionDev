'''
Practical Task 1 --- `strings.py`

Psuedocode:

Declare a variable as described in the assignment.
Use the the `strip()` method with `$` to remove the unwanted
characters from the string and print it.
'''

# Declare variable `hero` as a string.
hero = "$$$Superman$$$"

# Print the string stored in `hero`, with the '$' characters
# removed.
print(hero.strip("$"))